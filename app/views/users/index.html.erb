<h1>Here are all the users of Poster!</h1>


<ul>
	<% @users.each do |user| %>
	<li>

		<%= link_to user.username, user %> -
		<% if @logged_in_user_id == user.id %>
			<%= button_to "delete this account", user_path(user), method: :delete %> 
		<% end %>
	</li>
	<% end %>
</ul>