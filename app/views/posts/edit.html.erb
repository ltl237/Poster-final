<%= form_for @post do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title %>
	<br>
	<%= f.label :content %>
	<%= f.text_field :content %>
	<br>
	<%= f.hidden_field :user_id, value: @post.user_id %>
	<%= f.hidden_field :likes, value: 0 %>
	<%= f.submit %>
<% end %>